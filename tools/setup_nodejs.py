# -*- coding: utf-8 -*-
import os

package_list = [

]

for package in package_list:
    pass
    os.system(['npm install -S', package])
    # ini_filename = utils.get_path_with_base_file(__file__, '..')
    call() pwd='../'